@model IEnumerable<WebApp.Models.DepartmentViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Department</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<form asp-controller="Departments" asp-action="Index" method="get">
    <p>
        NameOfFaculty: <input type="text" name="SearchString">
        <input type="submit" value="Filter" />
    </p>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameOfDepartment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Faculty)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameOfDepartment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Faculty)
            </td>
            
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
